# ระบบ Auto-sync ใบแจ้งซ่อม → ประวัติเครื่องจักร

## 📋 สรุปการทำงาน

### 1. แจ้งซ่อม (Repair Form)
- ผู้ใช้แจ้งซ่อมผ่าน `repair_form.php`
- ข้อมูลถูกบันทึกลง **`mt_repair`** (ใบแจ้งซ่อม)
- สถานะเริ่มต้น: `10` (รออนุมัติ)

### 2. Workflow การแจ้งซ่อม
```
10 (รออนุมัติ) 
  ↓ อนุมัติ/ปฏิเสธ
20 (รอดำเนินการ)
  ↓ เริ่มซ่อม
30 (รออะไหล่) [ถ้าต้องการ]
  ↓ ได้อะไหล่
40 (ซ่อมเสร็จสิ้น) ⭐
  ↓ 🔥 AUTO-SYNC
mt_machine_history (ประวัติ)
```

### 3. Auto-sync Trigger
เมื่อสถานะเปลี่ยนเป็น **`40` (ซ่อมเสร็จสิ้น)** ระบบจะ:
- ✅ Auto-copy ข้อมูลจาก `mt_repair` → `mt_machine_history`
- ✅ Mapping ฟิลด์อัตโนมัติ
- ✅ ไม่ต้องบันทึกซ้ำ

## 📂 ไฟล์ที่เกี่ยวข้อง

### API Files
| ไฟล์ | หน้าที่ | Auto-sync |
|------|---------|-----------|
| `api/sync_repair_to_history.php` | ฟังก์ชัน sync หลัก | ✅ |
| `api/update_status.php` | อัปเดตสถานะใบแจ้งซ่อม | ✅ เรียก sync |
| `api/monitor_update.php` | อัปเดตสถานะจาก Monitor | ✅ เรียก sync |

### Frontend Files
- `pages/repair_form.php` - ฟอร์มแจ้งซ่อม
- `pages/machines.php` - จัดการเครื่องจักรและดูประวัติ

## 🗄️ Database Mapping

### จาก mt_repair → mt_machine_history

| mt_repair (แจ้งซ่อม) | mt_machine_history (ประวัติ) | หมายเหตุ |
|---------------------|------------------------------|----------|
| machine_number | machine_code | รหัสเครื่องจักร |
| machine_number | machine_name | ใช้ชั่วคราว (ควรดึงจาก mt_machines) |
| document_no | document_no | เลขที่เอกสาร |
| - | history_type | 'repair' (ค่าคงที่) |
| start_job | work_date | วันที่แจ้งซ่อม |
| start_date | start_date | วันที่เริ่มซ่อม |
| end_date | completed_date | วันที่ซ่อมเสร็จ |
| issue | issue_description | อาการเสีย |
| operation_detail | solution_description | วิธีแก้ไข |
| operation_detail | parts_used | อะไหล่ที่ใช้ |
| work_hours | work_hours | ชั่วโมงทำงาน |
| downtime_hours | downtime_hours | ชั่วโมงหยุดเครื่อง |
| total_cost | total_cost | ค่าใช้จ่ายรวม |
| reported_by | reported_by | ผู้แจ้ง |
| handled_by | handled_by | ผู้รับผิดชอบ |
| - | status | 'completed' (ค่าคงที่) |
| branch | branch | สาขา |
| department | department | แผนก |
| mtc_comment | note | หมายเหตุ |

## 🔧 การทำงานของ PM/Calibration

### PM (Preventive Maintenance)
- ❌ **ไม่ใช้** ฟอร์มแจ้งซ่อม
- ✅ **บันทึกตรงๆ** ผ่าน Modal ใน `machines.php` → แท็บ "ประวัติเครื่องจักร"
- เลือก `history_type = 'pm'`

### Calibration
- ❌ **ไม่ใช้** ฟอร์มแจ้งซ่อม
- ✅ **บันทึกตรงๆ** ผ่าน Modal ใน `machines.php` → แท็บ "ประวัติเครื่องจักร"
- เลือก `history_type = 'calibration'`

## 📊 ตัวอย่างการใช้งาน

### สถานการณ์ 1: เครื่องเสีย (Repair)
1. ผู้ใช้เปิด `repair_form.php` แจ้งซ่อม
2. MT Staff อนุมัติ → สถานะ 20
3. ช่างซ่อม → สถานะ 40 (เสร็จสิ้น)
4. 🔥 **ระบบ auto-sync** ไปยัง `mt_machine_history`

### สถานการณ์ 2: PM ตามแผน
1. MT Staff เปิด `machines.php` → แท็บ "ประวัติเครื่องจักร"
2. เลือกเครื่องจักร
3. กดปุ่ม "เพิ่มประวัติ"
4. เลือกประเภท = PM
5. กรอกข้อมูล → บันทึก
6. ✅ บันทึกตรงลง `mt_machine_history` เลย

### สถานการณ์ 3: Calibration
1. เหมือน PM แต่เลือกประเภท = Calibration

## 🧪 การทดสอบ

### ทดสอบ Auto-sync
```bash
# ทดสอบ sync ด้วย repair_id
curl "http://localhost/mt/api/sync_repair_to_history.php?repair_id=1"
```

### ตรวจสอบข้อมูล
```sql
-- ดูข้อมูลใบแจ้งซ่อมที่เสร็จแล้ว
SELECT * FROM mt_repair WHERE status = 40 ORDER BY id DESC LIMIT 5;

-- ดูว่า sync ไปประวัติแล้วหรือยัง
SELECT * FROM mt_machine_history ORDER BY id DESC LIMIT 5;

-- เช็คว่าเลขที่ตรงกันไหม
SELECT r.document_no, r.status, h.document_no, h.history_type
FROM mt_repair r
LEFT JOIN mt_machine_history h ON r.document_no = h.document_no
WHERE r.status = 40
LIMIT 10;
```

## ⚠️ ข้อควรระวัง

1. **Duplicate Check**: ถ้า `document_no` ซ้ำ จะ UPDATE แทน INSERT
2. **machine_name**: ตอนนี้ใช้ `machine_number` ชั่วคราว ควรดึงจาก `mt_machines.machine_name`
3. **parts_used**: ใช้ `operation_detail` แทน ถ้ามีฟิลด์เฉพาะควรเพิ่มใน `mt_repair`

## 📈 การปรับปรุงในอนาคต

- [ ] เพิ่มฟิลด์ `parts_used` ใน `mt_repair` เพื่อเก็บรายการอะไหล่แยกชัดเจน
- [ ] ดึง `machine_name` จาก `mt_machines` แทนการใช้ `machine_number`
- [ ] เพิ่ม Dashboard แสดงสถิติจากประวัติ
- [ ] Export รายงานประวัติการซ่อมเป็น Excel
- [ ] Alert ถ้า sync ไม่สำเร็จ
