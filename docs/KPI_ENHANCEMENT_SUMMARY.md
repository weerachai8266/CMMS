# KPI Dashboard Enhancement Summary
## สรุปการปรับปรุง KPI Dashboard

**วันที่:** 5 มกราคม 2026

---

## ✅ ฟีเจอร์ที่เพิ่มเข้ามาทั้งหมด

### 1. **Export & Print Functionality** 
- ✅ **Export to PDF** - สามารถส่งออกรายงาน KPI เป็น PDF ได้
- ✅ **Export to Excel** - ส่งออกข้อมูลเป็น Excel (หลายชีต)
- ✅ **Print Dashboard** - พิมพ์รายงานได้โดยตรง (มี @media print styles)

**Libraries ที่เพิ่ม:**
- jsPDF (v2.5.1) - สร้าง PDF
- html2canvas (v1.4.1) - จับภาพหน้าจอ
- SheetJS (xlsx v0.20.0) - สร้าง Excel

---

### 2. **KPI Cards เพิ่มเติม**
เพิ่ม KPI Cards ใหม่ 4 ตัว:

#### 🔵 **Success Rate (อัตราความสำเร็จ)**
- คำนวณจาก: (งานเสร็จสิ้น / งานทั้งหมด) × 100%
- แสดงเป็นเปอร์เซ็นต์

#### 🔵 **MTTR (Mean Time To Repair)**
- เวลาเฉลี่ยในการซ่อม (ชั่วโมง)
- คำนวณจาก: AVG(end_job - start_job)

#### 🟢 **Response Time (เวลาตอบสนอง)**
- เวลาเฉลี่ยในการอนุมัติ (นาที)
- คำนวณจาก: AVG(approved_at - start_job)

#### 🟡 **OEE (Overall Equipment Effectiveness)**
- ประสิทธิภาพโดยรวมของเครื่องจักร (%)
- สูตร: Availability × Quality
- Availability = (Total Time - Downtime) / Total Time
- Quality = Success Rate / 100

#### 🟣 **First Time Fix Rate (อัตราซ่อมสำเร็จครั้งแรก)**
- เปอร์เซ็นต์ของงานที่ซ่อมสำเร็จครั้งแรก
- คำนวณจาก: (งานสำเร็จ / งานทั้งหมด) × 100%

---

### 3. **Advanced Filters**
เพิ่มตัวกรองข้อมูล:
- ✅ **กรองตามแผนก** (Department Filter)
- ✅ **กรองตามสาขา** (Branch Filter)
- ✅ **กรองตามสถานะ** (Status Filter: รออนุมัติ, กำลังซ่อม, เสร็จสิ้น ฯลฯ)
- ✅ **ปุ่มล้างตัวกรอง** (Clear Filters Button)

**API Endpoint เพิ่ม:**
- `GET /api/kpi_data.php?action=get_filters` - โหลดรายการแผนก/สาขาทั้งหมด

---

### 4. **Comparison with Previous Period**
- ✅ เปรียบเทียบข้อมูลกับช่วงเวลาก่อนหน้า (ระยะเวลาเท่ากัน)
- ✅ แสดง **Trend Badges** (↑ สีเขียว = เพิ่มขึ้น, ↓ สีแดง = ลดลง)
- ✅ คำนวณ % เปลี่ยนแปลง

**Metrics ที่เปรียบเทียบ:**
- Total Repairs
- Success Rate
- MTTR (Lower is Better)
- Response Time (Lower is Better)
- First Time Fix Rate
- OEE

---

### 5. **Alert & Threshold Notifications**
แจ้งเตือนเมื่อ KPI ต่ำกว่าเกณฑ์:

#### 🔴 **Critical Alerts:**
- MTBF < 7 วัน → แสดง alert สีแดง
- Success Rate < 70% → แสดง alert สีเหลือง
- Pending Repairs > 10 รายการ → แสดง alert สีเหลือง

**Features:**
- แสดงที่มุมขวาบน
- Auto-dismiss หลัง 10 วินาที
- สามารถปิดด้วยตนเองได้

---

### 6. **Drill-Down Capabilities**
คลิกเพื่อดูรายละเอียดเพิ่มเติม:

#### 📊 **กราฟสถานะ (Status Chart)**
- คลิกที่กราฟ → แสดง Modal รายการแจ้งซ่อมตามสถานะ
- แสดงรายละเอียด: รหัสเครื่อง, แผนก, ปัญหา, วันที่แจ้ง

#### 🔧 **ตารางเครื่องจักร (Frequent Machines Table)**
- คลิกที่แถว → แสดงประวัติการซ่อมทั้งหมด
- แสดง: จำนวนครั้ง, ค่าใช้จ่ายรวม, เวลารวม, รายละเอียดแต่ละครั้ง

#### 📈 **ตาราง MTBF (MTBF Table)**
- คลิกที่แถว → แสดงประวัติการเสียและการซ่อม
- วิเคราะห์รูปแบบการเสีย

**API Endpoints เพิ่ม:**
- `GET /api/get_repair_details.php` - รายละเอียดตามสถานะ
- `GET /api/get_machine_history.php` - ประวัติการซ่อมตามเครื่อง

---

### 7. **Enhanced Tooltips**
ปรับปรุง Tooltips ในกราฟ:
- แสดงข้อมูลละเอียดมากขึ้น
- แสดงเปอร์เซ็นต์
- แสดงคำแนะนำ "คลิกเพื่อดูรายละเอียด"
- ใช้ฟอนต์ Sarabun
- สีพื้นหลังดำโปร่งใส
- Padding เพิ่มเติม

---

### 8. **Responsive Design Improvements**
ปรับปรุงการแสดงผลบนมือถือ:
- ✅ Header buttons แสดงเป็นคอลัมน์บนหน้าจอเล็ก
- ✅ Filter controls responsive
- ✅ KPI Cards แสดง 1 คอลัมน์บนมือถือ
- ✅ ตารางเลื่อนได้ (table-responsive)
- ✅ กราฟ responsive (maintainAspectRatio: false)
- ✅ Modal ขนาดใหญ่ปรับตามหน้าจอ

---

## 📁 ไฟล์ที่แก้ไข/สร้างใหม่

### แก้ไข:
1. `/var/www/html/mt/pages/kpi.php`
   - เพิ่ม libraries (jsPDF, html2canvas, xlsx)
   - เพิ่ม Export/Print buttons
   - เพิ่ม filter controls
   - เพิ่ม KPI Cards 8 ตัว
   - ปรับปรุง CSS (responsive, print, alerts, hover effects)

2. `/var/www/html/mt/assets/js/kpi.js`
   - เพิ่มฟังก์ชัน loadFilterOptions()
   - เพิ่มฟังก์ชัน clearFilters()
   - อัปเดต loadKPIData() รองรับ filters
   - อัปเดต updateKPICards() รองรับ comparison
   - เพิ่มฟังก์ชัน addTrendBadge()
   - เพิ่มฟังก์ชัน checkThresholdAlerts()
   - เพิ่มฟังก์ชัน displayAlerts()
   - เพิ่มฟังก์ชัน exportToPDF()
   - เพิ่มฟังก์ชัน exportToExcel()
   - เพิ่มฟังก์ชัน showStatusDetails() - drill-down
   - เพิ่มฟังก์ชัน showMachineHistory() - drill-down
   - ปรับปรุง tooltips ในกราฟทุกตัว

3. `/var/www/html/mt/api/kpi_data.php`
   - เพิ่ม action handler สำหรับ get_filters
   - รองรับ filter parameters (department, branch, status)
   - ใช้ dynamic WHERE clause
   - คำนวณ first_time_fix_rate
   - เพิ่มการเปรียบเทียบกับ previous period
   - Return comparison data

### สร้างใหม่:
4. `/var/www/html/mt/api/get_repair_details.php`
   - API สำหรับ drill-down รายละเอียดตามสถานะ
   - รับ parameters: status, date_from, date_to
   - Return รายการซ่อม (limit 100)

5. `/var/www/html/mt/api/get_machine_history.php`
   - API สำหรับดูประวัติการซ่อมตามเครื่อง
   - รับ parameter: machine_number
   - Return ประวัติ + สถิติ (ค่าใช้จ่ายรวม, เวลารวม)

---

## 🎨 การปรับปรุง UI/UX

### Colors & Styling:
- เพิ่ม gradient backgrounds
- เพิ่ม hover effects (scale + shadow)
- เพิ่ม animation (pulse สำหรับ alerts)
- ปรับ modal styling (gradient header)
- เพิ่ม cursor pointer บนตารางที่คลิกได้

### User Experience:
- Loading overlay ขณะโหลดข้อมูล
- Alert notifications แสดงข้อความเตือนชัดเจน
- Tooltip hints บนตาราง
- Modal แสดงข้อมูลละเอียด
- Responsive layout ใช้งานง่ายบนทุกอุปกรณ์

---

## 📊 การคำนวณ KPI

### MTTR (Mean Time To Repair)
```sql
AVG(TIMESTAMPDIFF(HOUR, start_job, end_job))
```

### Response Time
```sql
AVG(TIMESTAMPDIFF(MINUTE, start_job, approved_at))
```

### OEE (Overall Equipment Effectiveness)
```javascript
availability = (totalTime - downtimeHours) / totalTime
quality = successRate / 100
oee = availability × quality × 100
```

### Success Rate / First Time Fix Rate
```sql
(COUNT(status = 40) / COUNT(*)) × 100
```

### MTBF (Mean Time Between Failure)
```javascript
mtbf = totalPeriodHours / totalFailures
```

---

## 🔧 วิธีใช้งาน

### Export Functions:
1. **Export PDF:** คลิกปุ่ม "Export PDF" → รอสร้างไฟล์ → ดาวน์โหลดอัตโนมัติ
2. **Export Excel:** คลิกปุ่ม "Export Excel" → ได้ไฟล์ .xlsx (5 sheets)
3. **Print:** คลิกปุ่ม "Print" → เปิด print dialog

### Filters:
1. เลือกช่วงวันที่ (หรือใช้ปุ่มด่วน: วันนี้, สัปดาห์นี้, เดือนนี้ ฯลฯ)
2. เลือกแผนก (ถ้าต้องการ)
3. เลือกสาขา (ถ้าต้องการ)
4. เลือกสถานะ (ถ้าต้องการ)
5. คลิก "โหลดข้อมูล"
6. คลิก "ล้างตัวกรอง" เพื่อรีเซ็ต

### Drill-Down:
1. **กราฟสถานะ:** คลิกที่ส่วนของกราฟ → เห็น Modal รายละเอียด
2. **ตารางเครื่องจักร:** คลิกที่แถว → เห็นประวัติการซ่อม
3. **ตาราง MTBF:** คลิกที่แถว → เห็นประวัติการเสีย

---

## 🔄 API Changes Summary

### Modified Endpoint:
**GET /api/kpi_data.php**
- เพิ่ม query parameters:
  - `department` (optional)
  - `branch` (optional)
  - `status` (optional)
  - `action=get_filters` (สำหรับโหลดรายการ filters)

- เพิ่ม response fields:
  - `data.comparison` - ข้อมูลเปรียบเทียบ
  - `data.summary.first_time_fix_rate` - อัตราซ่อมสำเร็จครั้งแรก

### New Endpoints:
1. **GET /api/get_repair_details.php**
   - Parameters: `status`, `date_from`, `date_to`
   - Returns: รายการซ่อมตามสถานะ

2. **GET /api/get_machine_history.php**
   - Parameters: `machine_number`
   - Returns: ประวัติการซ่อม + สถิติ

---

## ✅ Testing Checklist

- [x] ทดสอบ Export PDF
- [x] ทดสอบ Export Excel
- [x] ทดสอบ Print
- [x] ทดสอบ Filters (แผนก, สาขา, สถานะ)
- [x] ทดสอบ Clear Filters
- [x] ทดสอบ KPI Cards ใหม่
- [x] ทดสอบ Trend Badges
- [x] ทดสอบ Alerts/Notifications
- [x] ทดสอบ Drill-down (คลิกกราฟ)
- [x] ทดสอบ Drill-down (คลิกตาราง)
- [x] ทดสอบ Responsive บนมือถือ
- [x] ทดสอบ Tooltips ในกราฟ

---

## 📝 Notes

- ไม่ได้เพิ่ม Real-time Updates ตามที่ user ระบุ
- Export PDF อาจใช้เวลาสักครู่ (ขึ้นอยู่กับขนาดหน้า)
- Drill-down จำกัดข้อมูลไม่เกิน 50-100 รายการ (performance)
- Alerts ตั้งค่า threshold เบื้องต้น (MTBF < 7, Success < 70%, Pending > 10)
- สามารถปรับ threshold ได้ใน function checkThresholdAlerts()

---

## 🚀 Next Steps (ถ้าต้องการพัฒนาต่อ)

1. เพิ่ม Goal/Target Settings (ตั้งเป้าหมาย KPI)
2. เพิ่ม Forecasting (ทำนายแนวโน้ม)
3. เพิ่ม Custom Date Comparison (เทียบกับช่วงใดก็ได้)
4. เพิ่ม Scheduled Reports (ส่งรายงานอัตโนมัติ)
5. เพิ่ม Dashboard Widgets (ปรับแต่งการแสดงผล)
6. เพิ่ม Export รูปแบบอื่น (CSV, JSON)

---

**Status:** ✅ Completed All Features (ยกเว้น Real-time Updates)
**Last Updated:** 5 มกราคม 2026
